{
  "name": "stealth-seller-watchers-mvp",
  "version": "0.1.0",
  "description": "Watchers sandbox: tRPC backend + the real v3 frontend build, one deploy",
  "private": true,
  "scripts": {
    "backend:dev": "cd backend && npm run dev",
    "backend:build": "cd backend && npm run build"
  },
  "dependencies": {
    "@trpc/server": "^10.45.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "superjson": "^1.13.3",
    "uuid": "^9.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  }
}
